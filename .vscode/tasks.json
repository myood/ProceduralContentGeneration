{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Complete rebuild",
            "type": "shell",
            "command": "./build.sh", // NOTE: Will require sudo-pass for the first time
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}