{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake gen & build",
            "type": "shell",
            "command": "rm -rf CMakeCache && rm -rf CMakeFiles && rm -rf cmake_install.cmake && rm -rf Makefile && cmake CMakeLists.txt && make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}